<h1>Editing supplier_stock_order</h1>

<%= form_for(@supplier_stock_order) do |f| %>
  <% if @supplier_stock_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier_stock_order.errors.count, "error") %> prohibited this supplier_stock_order from being saved:</h2>

      <ul>
      <% @supplier_stock_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      <%= f.hidden_field :product, :value => @supplier_stock_order.product.id %>


  <div class="field">
    <%= "Product" %><br />
    <%= @supplier_stock_order.product.name %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>

    <%= f.hidden_field :status, :value => @supplier_stock_order.status%>
  <div class="actions">
    <%= f.submit "Update Supplier Stock Order" , :class=>"btn btn-primary" %>
  </div>
<% end %>

<%= link_to 'Show', @supplier_stock_order %> |
<%= link_to 'Back', supplier_stock_orders_path %>
